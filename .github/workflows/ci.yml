name: my first pipeline

on: push

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
      - name: checkout code
        uses: actions/checkout@v4
        - name: docker build -t bkamche/ciportfolio .
        - name: list images
        - name: docker images
        - name: uptime

          run: uptime

        - name: check no of processors

          run: nproc

  test:
    needs: build

    runs-on: ubuntu-latest

    steps:
      

        - name: hello

          run: echo "Hello!"